在现有的task3（模型之间组装）的基础上，变成一个类似于task1的整网的组装，然后在已经插好桩的基础之上，按照组件级别的组件划分力度（17个组件），新补上一些插桩（用和原来的方式补上一些插桩），保证整网的时候能看到整体的loss是多少，然后它的插桩和蜕变分别是多少，以及在整网跑的时候给真实数据的情况下每个组件的输入和输出的张量是多少。

一共三组测试数据，分别是[MSA baseline], [PTA baseline]和[MSA 输入扰动后的结果]。每组测试数据包括每个组件的输入和输出的张量、整网输入和输出的张量。

差分测试是msa和pta的baseline，蜕变测试是msa的baseline和msa输入扰动后的结果。

最后，在五个层次（框架运行时的优化机制、分布式并行策略、训练超参数、数值精度格式、确定性控制）上做改动、改张量，然后重新跑整网。


一些更细节的补充说明:
1. pta蜕变的结果也记录下来吧，我们第一稿的时候不用，但是万一审稿人问到了，我们备用下，毕竟不需要太多额外的工作量来记录
2. Baseline选你现在调的精度能对的最准的一组参数设置，固定下来就ok，然后别忘了在文档里写一下你用的参数设置
3. 你刚才列的是一组实验要跑的数据，别忘了每个模型都要对应去插装。如果你实在是差不过来，那就先插几个dense的，插几个moe的模型，保证每个有代表性的类别都有几个典型模型。当然最好是目前已经实装的11个模型都能插装。
4. 关于参数配置: 记得参数配置定下来之后，除了探究RQ3的时候以外就不要再变了，我们原本每次要改两个参数，那个地方要b掉。
5. 关于RQ3探究影响因素: 那里实验组数比较多，不过没有新的代码需要写。你先把整网的部分追踪插桩都写好，然后我们看看跑一轮到底需要多久，最终一起再定: 到底是每个模型都探究一遍影响因素，还是只取一个典型的模型探究影响因素做消融？